{
  "name": "orbital",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "ORBITAL: THE LAST TEE — a gravity-golf game where the player sculpts trajectories. Milo, the last golf ball, plays the Grand Course.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pixi.js": "^8.6.6"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.6.3",
    "vite": "^6.0.0",
    "vitest": "^2.1.8"
  }
}
